# Niko Drone Simulator - AI 開發規範 (.aierules)

## 1. 版本控制與全域配置 (Versioning & Config)
- **版本基線**：目前的版本基線為 **1.6.0**。所有後續微調從 1.6.x 開始，重大功能從 1.7.0 開始。
- **配置變動**：嚴禁在未得到使用者明確許可前修改 `AppConfig.kt` 中的版本號、發布日期或管理員密碼。

## 2. 物理引擎與數據同步 (Physics & Logic)
- **架構解耦**：物理計算 (`PhysicsEngine.kt`) 必須與 UI 訊息生成解耦。物理引擎應回傳狀態標籤 (如 `CRASH_EXTREME`)，由 `DroneViewModel` 負責翻譯為用戶介面文字。
- **數據快照**：發生撞擊時，必須使用物理引擎在歸零前拍攝的 `impactSpeed` 快照，並在 `DroneViewModel` 中鎖定顯示，不得直接讀取歸零後的速度。
- **著陸標準**：預設執行「專業考核降落安全標準」(2.2m/s 損毀閾值)。若 `useStrictLanding` 為 OFF，則完全跳過硬著陸與地面傾角判定。

## 3. UI/UX 視覺規範 (UI Consistency)
- **戰術風格**：所有 HUD 與設定選單必須維持深色透明質感 (`Color(0xEE111111)`)，並帶有細微的白色邊框 (`0.1f` alpha)。
- **組件尺寸**：雷達與 OSD 視窗固定為 `150.dp x 100.dp`；姿態球固定為 `120.dp` 正圓。嚴禁使用統一父容器擠壓不同尺寸的組件。
- **按需顯示**：高干擾的 UI 元素 (如 L/R 迷你搖桿) 應僅在相關的分頁 (如 Controller Tab) 中顯示。
- **文字校正**：地面標題文字嚴禁出現鏡像反置，渲染時須確保 UV 座標正確。

## 4. 教學與考照功能 (Training & Mission)
- **定點計時**：實施雙重判定門檻：1) 垂直高度 1.0m ~ 3.5m；2) 水平半徑 1.3m (角錐) / 1.0m (H坪)。
- **障礙物實體化**：場地障礙物必須具備真實物理碰撞判定，且座標須與 `Constants.OBSTACLES` 同步。

## 5. 安全與權限 (Security)
- **專家模式**：預設為鎖定狀態。解鎖前必須隱藏所有底層技術參數欄位（如 PPS, Jitter, 模式切換）。
- **USB 主權**：偵測到 AX12 或 HID 設備插入時，優先跳轉至外接模式並鎖定背景掃描，除非使用者手動切換模式。
